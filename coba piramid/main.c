#include <stdio.h>
#include <stdlib.h>

int main()
    for(e = 1; e <= n; i++)
{
    for(f = 1; e <= 2*n - 1; f++)
    {
        if (e >= n-(i-1) && <= n+ (i-1))


   {
       printf("*");
   }
   else
    {
        printf(" ");
    }
   printf("\n");
    return 0;
}
